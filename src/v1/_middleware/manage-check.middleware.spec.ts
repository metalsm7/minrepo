import { ManageCheckMiddleware } from './manage-check.middleware';

describe('ManageCheckMiddleware', () => {
  it('should be defined', () => {
    expect(new ManageCheckMiddleware()).toBeDefined();
  });
});
